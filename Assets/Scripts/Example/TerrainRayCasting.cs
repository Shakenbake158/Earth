using UnityEngine;

public class TerrainRayCasting : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //TerrainTile
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
